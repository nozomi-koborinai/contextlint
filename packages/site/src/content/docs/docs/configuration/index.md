---
title: Configuration
description: The contextlint config file and its options.
---

contextlint's behavior is controlled by `contextlint.config.json` at the repository root. This category covers how to write the config file, how to specify what gets validated, and how to scope rules at a per-file granularity.

## In this category

- [Config file](/docs/configuration/config-file/) — Structure of `contextlint.config.json` and an overview of each field
- [Include patterns](/docs/configuration/include-patterns/) — Specifying which files to validate, and the precedence order
- [Per-file rule scoping](/docs/configuration/per-file-rule-scoping/) — Applying a rule to only a subset of files
- [Config auto-detection](/docs/configuration/auto-detection/) — How `findConfig` walks up parent directories
